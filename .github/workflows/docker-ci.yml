name: Test Workflow

on:
  push:
    branches:
      - fota-server

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test step
        run: echo "Workflow is working!"
